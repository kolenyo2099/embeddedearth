[pytest]
testpaths = tests
markers =
    slow: requires model download (~1-2 GB); skipped by default
    gee: requires Google Earth Engine credentials; skipped by default
addopts = -m "not slow and not gee"
